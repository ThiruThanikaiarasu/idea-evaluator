# Local bridge settings
IDEA_EVALUATOR_HOST=127.0.0.1
IDEA_EVALUATOR_PORT=8787

# Optional provider key for the Python CLI (never commit a real key)
GROQ_API_KEY=

# Vercel/hosted UI settings
VITE_DEPLOYMENT_TARGET=local
VITE_API_BASE=http://127.0.0.1:8787
