[project]
name = "agent-panel-demo"
version = "0.1.0"
description = "A safe, observable multi-agent product-critique workshop demo"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
